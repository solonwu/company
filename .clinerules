# Spring Boot 3 與 Angular 專案 Agent 執行規則

## 1. 核心行為準則 (Core Operating Principles)
- **單次專注單一任務**：一次只處理一個具體任務，嚴禁在單一輪次中同時修改前後端多個不相關檔案。
- **嚴格編譯驗證**：修改完程式碼後，必須執行編譯/建置指令（如 `mvn compile` 或 `ng build`），確認沒有語法或型態錯誤後才能回報完成。
- **針對性除錯**：若建置失敗，仔細讀取報告中的原生 Error Log，針對報錯檔案與行數進行精準修復，不要隨意重構無關的程式碼。
- **精簡回應**：保持說明簡短，專注於提供可直接運行的高品質程式碼與關鍵註解。

---

## 2. 後端技術規範 (Spring Boot 3 + Java 21)
- **開發環境**：Java 21（適當運用 Record、Pattern Matching 等新特性）。
- **核心框架**：Spring Boot 3.x、Spring Data JPA、Lombok。
- **架構規範**：
  - `entity/`：精準映射 SQL 資料表，欄位名稱需與 SQL DDL 大小寫一致。
  - `repository/`：繼承 `JpaRepository` 或 `JpaSpecificationExecutor`。
  - `dto/`：統一使用 Java `record` 或 Lombok `@Data` 作為 API Request/Response 資料模型。
  - `service/` 與 `service/impl/`：業務邏輯必須介面與實作分離。
  - `controller/`：提供 RESTful API，路徑統一為 `@RequestMapping("/api/...")`。
- **API 回傳格式**：統一使用包裝類別 `ResponseEntity<ApiResponse<T>>`。
- **資料庫**：MS SQL Server（資料庫名稱：`FATCA`）。

---

## 3. 前端技術規範 (Angular Standalone 架構)
- **核心框架**：Angular 18+，採用獨立組件架構 (`standalone: true`)。
- **模組匯入規則（極重要）**：
  - 必須從 `@angular/common` 或 `@angular/forms` 匯入 `CommonModule`、`FormsModule`、`ReactiveFormsModule`。
  - 絕對禁止使用 `@common` 等無效的路徑別名。
- **HTTP 通訊規範**：
  - 使用 `inject(HttpClient)` 或建構子注入 `HttpClient`。
  - API Base URL 預設為 `http://localhost:8080/api`。
  - 呼叫 `HttpClient` 時，除非需要監聽檔案上傳進度，否則不要傳入 `observe: 'events'`，回傳型態一律保持為 `Observable<T>`。
- **資料夾結構**：
  - `core/`：存放全域 Service、HTTP Interceptor、Guards。
  - `features/`：存放業務功能模組 UI Component。
  - `shared/`：存放可跨模組複用的 UI 組件、Pipes、Directives。

---

## 4. 品質檢查與執行流程
當接收到撰寫或修改程式碼的指令時，請嚴格遵守以下順序：

1. **後端驗證**：修改 Java 程式碼後，至後端目錄執行 `mvn compile` 或 `./mvnw compile`。
2. **前端驗證**：修改 Angular 程式碼後，至 `frontend` 目錄執行 `ng build`。
3. **自我校正**：若出現 TS2322、TS2307 或 Java Compilation Error，立即修正對應檔案並重新執行編譯驗證。